int fatorial(int n) {
	if(n <= 1) {
		return 1;
	} else {
		int aux;
	
		aux = +n-1;
		return fatorial(aux);
	}
}

main {
	int var, resultado;
	
	var = 3;
	resultado = fatorial(var);
	
	write("Fatorial(" + var + ") = " + resultado);
}